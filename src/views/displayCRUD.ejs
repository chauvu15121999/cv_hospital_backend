<!DOCTYPE html>
<html>
<style>
table, th, td {
  border:1px solid black;
}
</style>
<body>

    <h2>A basic HTML table</h2>

    <table style="width:100%; align-items: center;">
    <tr>
        <th>Email</th>
        <th>First Name</th>
        <th>Last Name</th>
        <th>Phone</th>
        <th>Action</th>
    </tr>
    <% for(let i = 0; i < dataTable.length; i++ ) { %>
        <tr>
            <th><%= dataTable[i].email %></th>
            <th><%= dataTable[i].firstName %></th>
            <th><%= dataTable[i].lastName %></th>
            <th><%= dataTable[i].phoneNumber %></th>
            <th>
                <a href="/edit-crud?id=<%= dataTable[i].id %>"><button class="btn-edit" type="button">Edit</button></a>
                <a href="/delete-crud?id=<%= dataTable[i].id %>"><button class="btn-edit" type="button">Delete</button></a>
            </th>
        </tr>
    <% }  %>
    </table>

    <p>To undestand the example better, we have added borders to the table.</p>

</body>
</html>